{
  "frontend/src/App.tsx": "Main application component with TanStack Router and authentication guard — now renders LoginScreen immediately without any blocking spinner or initialization timeout, showing the authenticated layout only after identity is confirmed.",
  "frontend/src/pages/InternationalMatch.tsx": "International cricket match page with integrated 3D stadium view — fixed TypeScript errors by using the correct `TeamData.color` field (instead of the non-existent `jerseyColor`) when setting team colors for the 3D renderer, and removed the unused `jerseyColorToHex` helper function.",
  "frontend/src/pages/IPLTournament.tsx": "IPL tournament page with all 10 franchise teams, round-robin group stage simulation, points table, and knockout rounds (Qualifier 1, Eliminator, Qualifier 2, Final) with champion display.",
  "frontend/src/pages/BBLTournament.tsx": "BBL tournament page with all 8 Australian franchise teams, round-robin group stage simulation, points table, and finals series (Semi Finals + Final) with champion display.",
  "frontend/src/pages/DomesticTournament.tsx": "Domestic cricket tournament page with league selector for Ranji Trophy, Sheffield Shield, and County Championship, each with their own teams, season simulation, points table, and champion display.",
  "frontend/src/lib/matchSimulator.ts": "Cricket match simulation engine with ball-by-ball logic, weighted random outcomes, commentary generation, innings state management, and full innings simulation supporting all formats.",
  "frontend/src/lib/authConfig.ts": "Environment-aware authentication configuration that returns the correct Internet Identity provider URL for local vs production ICP deployments, with added console logging for production debugging.",
  "frontend/src/main.tsx": "Application entry point that initializes React with a single top-level `QueryClientProvider` and `InternetIdentityProvider`, with a shared `QueryClient` configured with sensible retry and stale-time defaults.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations including user profile management, team queries, tournament teams/matches, tournament result recording (the newly added `useRecordTournamentResult`), and match creation with proper cache invalidation.",
  "frontend/src/components/LoginScreen.tsx": "Login page with stadium banner, cricket branding, and Internet Identity login button — now renders and enables the login button immediately on load with no initialization blocking, only disabling the button during an active login attempt.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal overlay for first-time users to set up their player profile name, with form validation and loading state during save.",
  "frontend/src/components/Header.tsx": "Sticky navigation header with logo, nav links to all game modes, user display name from profile, and logout button that clears identity and React Query cache.",
  "frontend/src/pages/Dashboard.tsx": "Main dashboard page showing a greeting with the user's profile name, game mode selection cards for IPL/BBL/International/Domestic, and a how-to-play guide.",
  "frontend/src/components/CricketStadium3D.tsx": "3D cricket stadium component using React Three Fiber — fixed the TypeScript `never[]` inference error by adding an explicit type annotation `{ x: number; z: number; angle: number }[]` to the `items` array in the `StadiumStands` useMemo callback."
}